export const enum SubscriptionObjectType {
  Activity = "activity",
  Athlete = "athlete",
}

export const enum SubscriptionActionType {
  Create = "create",
  Update = "update",
  Delete = "delete",
}
